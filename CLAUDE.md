# CLAUDE.md

MyAthanor は個人用のリソース・アイデア管理ツール。仕様・データモデル・集計ルールは
[README.md](README.md) に書いてある。**作業前に README を読むこと。**
ここには README に書いていない、実装するうえでの決まりだけを置く。

## 動かす

```bash
npm install
npm start        # http://0.0.0.0:3000
npm run dev      # ファイル変更で自動再起動
```

テストフレームワークは入れていない。確認はサーバを起動して API を叩くか、
ブラウザで実際に触ること。「たぶん動く」で終わらせない。

## スキーマを変えるとき（重要）

**本番DBは持ち主のPCにある `data/myathanor.db` 1個だけ**で、消さずに使い続けている。
サンドボックスや初回起動では空のDBが作られるため、
**「手元では動いたが持ち主のPCでは列が無くて落ちる」が起こりやすい。**

- 既存テーブルへの列追加は、必ず [src/db.js](src/db.js) の `addColumn()` を使う。
  `CREATE TABLE IF NOT EXISTS` は既存DBの列を増やさない。
- 新しい列は `NOT NULL` にするなら既定値を必ず付ける（既存行が埋まらないため）。
- 列の削除・リネーム・型変更はしない。使わなくなった列は放置してよい。
- マイグレーションを書いたら、空のDBと既存DBの両方で起動が通ることを確認する。
  既存DBの代用は「変更前のコードで一度起動してDBを作り、変更後のコードで起動し直す」。

## 構成と書き方

```
server.js      Express（API + 静的配信 + SPA フォールバック）
src/db.js      SQLite 接続・スキーマ・マイグレーション
src/store.js   データアクセスと状態遷移。ビジネスロジックはここに集める
src/routes.js  REST API。検証とHTTPへの変換だけを持つ
src/period.js  週（月曜始まり）／月の期間と日付ユーティリティ
public/        Web UI
```

- **依存を増やさない。** 実行時の依存は Express のみ。SQLite は Node 標準の
  `node:sqlite`（`DatabaseSync`、同期API）を使い、ネイティブビルドを発生させない。
  Node 22.5 以上が前提。
- **ビルド工程を持ち込まない。** `public/` は素の ES モジュールで、`index.html` が
  `/app.js` を直接読む。バンドラ・TypeScript・フレームワークを追加しない。
- **エラーは `err.status` を付けて投げる。** `store.js` は同期的に throw し、
  `routes.js` の `handle()` が拾って JSON に落とす。ルート側で try/catch を書かない。
- 単位は DB に**分**と**円**の整数で入れる。時間への変換は UI 側の仕事。
- コメント・コミットメッセージ・UI 文言は日本語。コメントは「何をしたか」ではなく
  **なぜそうしたか**を書く（既存のコメントがその調子なので合わせる）。

## 認証は無い（意図的）

このアプリはログインを持たない。Tailscale のネットワーク層だけが防御で、
`HOST=0.0.0.0` はそのための既定値。**認証や公開機能を勝手に足さない。**
逆に、秘密情報をDBに入れる前提の機能も追加しない。
